class Entities {
  constructor() { }

  setGraphics(graphic) {
    this.Graphics = graphic;
  }

  setDebug(debug) {
    this.Debug = debug;
  }

  update() { }

  dispose() { }

  init() { }

  registerDebugger() { }
}

export default Entities;
